# LOG — Append-Only Activity Log

Every significant action gets a `##` entry with an ISO-8601 timestamp (Asia/Dhaka). Newest entries at the bottom. Written by `@activity-log` and the merge step of parallel waves.

---
